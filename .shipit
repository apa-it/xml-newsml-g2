steps = FindVersion, ChangeVersion, ChangePodVersion, Readme, CheckChangeLog, CheckVersionsMatch, DistTest, Commit, Tag, MakeDist, UploadCPAN

git.tagpattern = NewsML-G2-%v

CheckChangeLog.files = Changes
